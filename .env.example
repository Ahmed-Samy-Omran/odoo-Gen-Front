VITE_API_URL=http://localhost:8000

# Supabase auth (optional). When set, the login screen shows "Continue with Google".
# Create a project at https://supabase.com, enable Google as an auth provider, and add
# your app URL (http://localhost:5173) to the allowed redirect URLs.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
